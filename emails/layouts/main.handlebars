<html>
  <head>
    <link rel="icon" type="image/png" href="/images/favicon.png" />
    <title>{{pageTitle}}</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <style>
    body { margin: 0; font-family: "Roboto", sans-serif; color: #021632; }
    .main-container { height: 100vh; width: 100vw; background-color: #021632;
    display: flex; justify-content: center; } .contained { display: flex;
    flex-direction: column; width: 30%; height: fit-content; } .card { height:
    fit-content; background-color: #e4ebf8; width: 100%; position: relative;
    box-shadow: 0 4px 8px 0 rgba(255, 255, 255, 0.2), 0 6px 20px 0 rgba(0, 255,
    221, 0.1); box-sizing: border-box; padding: 1.5rem; display: flex;
    flex-direction: column; line-height: 2rem; } .card-header { font-size:
    2.5rem; font-weight: 700; } .card-content { font-size: 1.5rem; padding:
    1.5rem 0rem; } .card-button { width: 15rem; height: 4rem; background:
    #21e6c1; color: #021632; font-size: 1.25rem; font-weight: 700;
    text-transform: uppercase; border-radius: 5px; align-self: center; border:
    0; cursor: pointer; } .card-button:hover { background: #23d8b7; transition:
    all 150ms ease-in-out; } .card-footer { height: 1.5rem; width: 100%;
    position: absolute; left: 0; bottom: 0; } .outer-container { height:
    fit-content; width: 100%; display: flex; flex-direction: row; align-items:
    center; } .outer-container-image { width: 5rem; height: 5rem; color:
    #e4ebf8; } .outer-container-text { color: #e4ebf8; font-size: 2.5rem;
    font-weight: 700; padding-top: 2.5rem; padding-bottom: 2.5rem; padding-left:
    2.5rem; } .error{background-color: #FD5066; } .success{ background-color:
    #21E6C1;}

  </style>
  <body>
    <div class="main-container">
      <div class="contained">
        <div class="outer-container">
          <img
            class="outer-container-img"
            src="/images/app-ico-image.png"
            alt="app-ico-image"
          />
          <div class="outer-container-text">{{appTitle}}</div>
        </div>
        <div class="card">
          <div class="card-header">{{cardTitle}}</div>
          <div class="card-content">{{{cardContent}}}</div>
          {{!-- <button class="card-button">{{buttonText}}</button> --}}

          <div class="card-footer {{contentState}}"></div>

        </div>
      </div>
    </div>
  </body>
</html>